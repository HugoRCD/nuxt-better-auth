# ================================
# Required Environment Variables
# ================================

# Better Auth secret key (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET=your-secret-key-here

# Your application URL (change to your production URL when deploying)
BETTER_AUTH_URL=http://localhost:3000

# ================================
# Docker Compose Configuration
# ================================

# Application port (default: 3000)
APP_PORT=3000

# PostgreSQL configuration
POSTGRES_PASSWORD=postgres
POSTGRES_DB=nuxthub

# ================================
# Optional: OAuth Providers
# ================================

# GitHub OAuth (https://github.com/settings/applications/new)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ================================
# Note: These are auto-configured in docker-compose
# Only set these if using external services
# ================================

# Database URL (required for NuxtHub to use external PostgreSQL)
# DATABASE_URL=postgresql://postgres:postgres@db:5432/nuxthub

# Redis URL for session storage
# REDIS_URL=redis://redis:6379
